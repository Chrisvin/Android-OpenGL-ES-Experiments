<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jem.openglexperiment">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".opengl.experiments.experiment1.GLActivity1" />
        <activity android:name=".opengl.experiments.experiment2.GLActivity2" />
        <activity android:name=".opengl.experiments.experiment3.GLActivity3" />
        <activity android:name=".opengl.experiments.experiment4.GLActivity4" />
        <activity android:name=".opengl.experiments.experiment5.GLActivity5" />
        <activity android:name=".opengl.experiments.experiment6.GLActivity6" />
        <activity android:name=".opengl.experiments.experiment7.GLActivity7" />
        <activity android:name=".opengl.experiments.experiment8.GLActivity8" />
        <activity android:name=".opengl.experiments.experiment9.GLActivity9" />
    </application>

</manifest>